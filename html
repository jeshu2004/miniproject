<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mini Dashboard</title>
    <link rel="stylesheet" href="styles.css" />
</head>

<body class="light">

    <h1 class="title">Mini Dashboard</h1>

    <!-- InputField Component -->
    <section class="section">
        <h2>InputField Example</h2>
        <div class="input-wrapper">
            <label for="username" class="input-label">Username</label>
            <div class="input-container">
                <input id="username" type="text" class="input-field outlined md" placeholder="Enter your username" aria-describedby="username-help" />
                <button class="clear-btn hidden" onclick="clearInput('username')">×</button>
                <div class="loading-spinner hidden" id="username-loader"></div>
            </div>
            <small id="username-help" class="helper-text">Helper text goes here</small>
            <small class="error-text hidden">This field is required</small>
        </div>

        <!-- Password Input -->
        <div class="input-wrapper">
            <label for="password" class="input-label">Password</label>
            <div class="input-container">
                <input id="password" type="password" class="input-field outlined md" placeholder="Enter your password" />
                <button class="toggle-btn" onclick="togglePassword()">Show</button>
            </div>
        </div>

        <!-- Dark Mode Toggle -->
        <button onclick="toggleTheme()" class="theme-toggle">Toggle Dark Mode</button>
    </section>

    <!-- DataTable Component -->
    <section class="section">
        <h2>DataTable Example</h2>
        <div id="table-container">
            <div id="loading" class="loading hidden" role="status">Loading data...</div>
            <div id="empty" class="empty hidden">No data available</div>

            <table class="data-table hidden" id="data-table" role="table" aria-label="User table">
                <thead>
                    <tr>
                        <th><input type="checkbox" id="select-all" onclick="toggleSelectAll(this)" aria-label="Select all rows"></th>
                        <th onclick="sortTable(1)">Name ⬍</th>
                        <th onclick="sortTable(2)">Email ⬍</th>
                    </tr>
                </thead>
                <tbody id="table-body">
                    <!-- rows injected by JS -->
                </tbody>
            </table>
        </div>
    </section>

    <script src="data-table.js"></script>
</body>

</html>
